import { router, useLocalSearchParams } from 'expo-router'; import { Pressable, SafeAreaView, ScrollView, StyleSheet, Text } from 'react-native'; import { MissionQRCode } from '@/components/MissionQRCode'; import { Colors } from '@/constants/Colors'; import { useMission } from '@/hooks/useMission';
export default function ClientConfirmation() { const { id = 'docs-02' } = useLocalSearchParams<{ id: string }>(); const mission = useMission(id); return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Text style={styles.title}>Confirme a sua missão</Text><Text style={styles.copy}>Mostre este código ou QR ao Nengue para validar a conclusão.</Text><MissionQRCode mission={mission} /><Pressable onPress={() => router.replace('/(client)/home')} style={styles.button}><Text style={styles.buttonText}>Voltar à Home</Text></Pressable></ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#FCF9F8' }, content: { padding: 24, paddingTop: 70 }, title: { color: Colors.light.text, fontSize: 28, fontWeight: '800', textAlign: 'center' }, copy: { color: Colors.light.icon, textAlign: 'center', lineHeight: 22, marginTop: 10, marginBottom: 24 }, button: { width: '100%', height: 54, backgroundColor: Colors.light.secondary, borderRadius: 13, alignItems: 'center', justifyContent: 'center', marginTop: 25 }, buttonText: { color: '#FFF', fontWeight: '800', fontSize: 16 } });