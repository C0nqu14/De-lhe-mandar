import { router } from 'expo-router'; import { Pressable, SafeAreaView, StyleSheet, Text, TextInput } from 'react-native'; import { Colors } from '@/constants/Colors'; import { missionDraft } from '@/services/missionDraft';
export default function PurchaseValue() { return <SafeAreaView style={styles.safe}><Text style={styles.progress}>5 DE 6</Text><Text style={styles.title}>Qual o valor estimado?</Text><Text style={styles.copy}>Este valor será usado apenas como estimativa de compra.</Text><TextInput value={String(missionDraft.purchaseAmount || '')} onChangeText={(value) => { missionDraft.purchaseAmount = Number(value) || 0; }} keyboardType="numeric" placeholder="Valor em Kz" placeholderTextColor="#999" style={styles.input} /><Pressable style={styles.button} onPress={() => router.push('/(client)/create-mission/price')}><Text style={styles.buttonText}>Continuar</Text></Pressable></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#FCF9F8', padding: 20, paddingTop: 60 }, progress: { color: Colors.light.secondary, fontWeight: '800' }, title: { color: Colors.light.text, fontSize: 30, fontWeight: '800', marginTop: 22 }, copy: { color: Colors.light.icon, fontSize: 16, marginVertical: 20 }, input: { backgroundColor: '#FFF', borderWidth: 1, borderColor: '#E5E2E1', borderRadius: 12, padding: 15, minHeight: 54, color: Colors.light.text }, button: { height: 54, backgroundColor: Colors.light.secondary, borderRadius: 13, alignItems: 'center', justifyContent: 'center', marginTop: 28 }, buttonText: { color: '#FFF', fontWeight: '800', fontSize: 16 } });