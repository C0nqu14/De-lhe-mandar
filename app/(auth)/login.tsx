import { router } from 'expo-router';
import { Pressable, SafeAreaView, StyleSheet, Text, View } from 'react-native';
import { Colors } from '@/constants/Colors';
import { sessionService } from '@/services/sessionService';

export default function LoginScreen() { const enter = (role: 'CLIENT' | 'EXECUTOR') => { sessionService.signIn(role); router.replace(role === 'CLIENT' ? '/(client)/home' : '/(executor)/dashboard'); }; return <SafeAreaView style={styles.safe}><View style={styles.content}><Text style={styles.logo}>De Lhe Mandar</Text><Text style={styles.title}>Entre na sua conta</Text><Text style={styles.copy}>Venda tempo, não entregas.</Text><Pressable style={styles.client} onPress={() => enter('CLIENT')}><Text style={styles.buttonText}>Entrar como Cota</Text></Pressable><Pressable style={styles.executor} onPress={() => enter('EXECUTOR')}><Text style={styles.buttonText}>Entrar como Nengue</Text></Pressable><Pressable onPress={() => router.push('/(auth)/register')}><Text style={styles.link}>Criar uma conta</Text></Pressable></View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: '#FCF9F8' }, content: { padding: 24, paddingTop: 90 }, logo: { color: Colors.light.tint, fontSize: 24, fontWeight: '800' }, title: { color: Colors.light.text, fontSize: 31, fontWeight: '800', marginTop: 65 }, copy: { color: Colors.light.icon, fontSize: 16, marginTop: 8, marginBottom: 36 }, client: { backgroundColor: Colors.light.tint, height: 56, borderRadius: 13, alignItems: 'center', justifyContent: 'center' }, executor: { backgroundColor: Colors.light.secondary, height: 56, borderRadius: 13, alignItems: 'center', justifyContent: 'center', marginTop: 13 }, buttonText: { color: '#FFF', fontSize: 16, fontWeight: '800' }, link: { color: Colors.light.tint, textAlign: 'center', marginTop: 28, fontWeight: '700' } });