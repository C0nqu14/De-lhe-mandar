import { supabase } from '@/lib/supabase';
import { missionService } from '@/services/missionService';
import { Mission } from '@/types/mission';
import { useEffect, useState } from 'react';

export function useMissions() { const [items, setItems] = useState<Mission[]>(missionService.list()); useEffect(() => { void missionService.refresh().then(setItems).catch(() => undefined); const unsubscribe = missionService.subscribe(() => setItems(missionService.list())); const channel = supabase.channel('missions-realtime').on('postgres_changes', { event: '*', schema: 'public', table: 'missions' }, () => { void missionService.refresh().catch(() => undefined); }).on('postgres_changes', { event: '*', schema: 'public', table: 'mission_checkpoints' }, () => { void missionService.refresh().catch(() => undefined); }).on('postgres_changes', { event: '*', schema: 'public', table: 'mission_executor_locations' }, () => { void missionService.refresh().catch(() => undefined); }).subscribe(); return () => { unsubscribe(); void supabase.removeChannel(channel); }; }, []); return items; }
export function useMission(id: string) { const [mission, setMission] = useState<Mission>(() => missionService.get(id)); useEffect(() => { void missionService.refresh().then(() => setMission(missionService.get(id))).catch(() => undefined); const unsubscribe = missionService.subscribe(() => setMission(missionService.get(id))); const channel = supabase.channel(`mission-${id}`).on('postgres_changes', { event: '*', schema: 'public', table: 'missions', filter: `id=eq.${id}` }, () => { void missionService.refresh().catch(() => undefined); }).on('postgres_changes', { event: '*', schema: 'public', table: 'mission_checkpoints', filter: `mission_id=eq.${id}` }, () => { void missionService.refresh().catch(() => undefined); }).on('postgres_changes', { event: '*', schema: 'public', table: 'mission_executor_locations', filter: `mission_id=eq.${id}` }, () => { void missionService.refresh().catch(() => undefined); }).subscribe(); return () => { unsubscribe(); void supabase.removeChannel(channel); }; }, [id]); return mission; }